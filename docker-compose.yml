services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./billing.db
      WEBHOOK_SECRET: dev-secret
      ENVIRONMENT: development
    volumes:
      - .:/app
